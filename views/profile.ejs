<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
</head>

<body>
    <h1>
        <%= pet.name %>'s Profile
    </h1>
    <img src="<%= pet.url %>" alt="<%= pet.name %>">
    <p>Breed: <%= pet.breed %>
    </p>
    <p>Age: <%= pet.age %> years</p>
    <p>Description: <%= pet.description %>
    </p>
    <a href="/pets/<%= pet.breed.toLowerCase() %>">Back to <%= pet.breed %> list</a>
    <a href="/">Go back to homepage</a>
</body>

</html>